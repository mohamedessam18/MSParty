{
  "name": "@msparty/extension",
  "private": true,
  "scripts": {
    "build": "esbuild src/background.ts src/content-script.ts src/popup.ts src/netflix-page.ts --bundle --outdir=dist --format=iife --target=chrome110 && cp src/manifest.json src/popup.html dist/",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "esbuild": "^0.28.1",
    "typescript": "^5.5.4"
  }
}

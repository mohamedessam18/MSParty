"use client";
import Link from "next/link";
import { useRouter } from "next/navigation";
import { useState } from "react";
export default function Register() { const router = useRouter(); const [name, setName] = useState(""); const [email, setEmail] = useState(""); const [password, setPassword] = useState(""); const [error, setError] = useState(""); async function submit(event: React.FormEvent) { event.preventDefault(); const response = await fetch("/api/auth/register", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ name, email, password }) }); if (!response.ok) return setError("استخدم بريدًا مختلفًا وكلمة مرور من ٨ أحرف على الأقل."); router.push("/login"); } return <main className="mx-auto flex min-h-screen max-w-md items-center px-5"><section className="w-full rounded-[26px] border border-[#d4b7ff]/20 bg-[#131d35]/80 p-6 shadow-2xl shadow-black/20 sm:p-9"><Link className="display text-xl font-bold" href="/">MS<span className="text-[#90e4ff]">Party</span></Link><h1 className="display mt-8 text-3xl font-semibold">اعمل مكان للسهرة.</h1><p className="mt-2 text-sm text-[#aab9d7]">ثلاث خانات، وبعدها تبقى جاهز تدعو صحابك.</p><form className="mt-7 space-y-4" onSubmit={submit}>{[["الاسم", name, setName, "text"], ["البريد الإلكتروني", email, setEmail, "email"], ["كلمة المرور", password, setPassword, "password"]].map(([label, value, setter, type]) => <label className="block text-sm text-[#cbd8f1]" key={label as string}>{label as string}<input required minLength={type === "password" ? 8 : undefined} className="mt-2 w-full rounded-xl border border-white/10 bg-[#0d1629] px-4 py-3 text-white" type={type as string} value={value as string} onChange={event => (setter as (value: string) => void)(event.target.value)} /></label>)}{error && <p className="rounded-xl bg-[#ff7b8d]/15 px-3 py-2 text-sm text-[#ffd6dd]">{error}</p>}<button className="w-full rounded-xl bg-[#d4b7ff] px-4 py-3 font-bold text-[#10172b] hover:bg-[#e5d4ff]">اعمل حساب</button></form><Link className="mt-5 block text-sm text-[#90e4ff]" href="/login">عندك حساب؟ ادخل من هنا</Link></section></main>; }

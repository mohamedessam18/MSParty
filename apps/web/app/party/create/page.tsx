"use client";
import Link from "next/link";
import { useRouter } from "next/navigation";
import { useState } from "react";
export default function CreateParty() { const router = useRouter(); const [name, setName] = useState(""); const [contentUrl, setContentUrl] = useState(""); const [error, setError] = useState(""); async function submit(event: React.FormEvent) { event.preventDefault(); const response = await fetch("/api/parties", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ name, contentType: "youtube", contentUrl }) }); if (!response.ok) return setError("مش قادرين نعمل البارتي دلوقتي. تأكد إنك مسجل دخول وجرب تاني."); router.push(`/party/${(await response.json()).id}`); } return <main className="mx-auto min-h-screen max-w-2xl px-5 py-7"><Link className="display text-xl font-bold" href="/">MS<span className="text-[#90e4ff]">Party</span></Link><section className="mt-14"><p className="mono text-xs text-[#90e4ff]">HOST A NIGHT</p><h1 className="display mt-2 text-4xl font-semibold">افتح الشاشة للشلة.</h1><p className="mt-3 text-[#aab9d7]">اختار فيديو YouTube، وبعدها ابعت الكود لصحابك.</p><form onSubmit={submit} className="mt-8 space-y-6 rounded-[24px] border border-white/10 bg-[#131d35]/70 p-5 sm:p-7"><label className="block text-sm text-[#cbd8f1]">اسم السهرة<input required className="mt-2 w-full rounded-xl border border-white/10 bg-[#0d1629] px-4 py-3 text-white" placeholder="مثال: ليلة فيلم الجمعة" value={name} onChange={event => setName(event.target.value)} /></label><div className="rounded-2xl border border-[#90e4ff]/20 bg-[#90e4ff]/5 p-4"><span className="text-xl text-[#90e4ff]">▶</span><b className="mr-2">YouTube فقط</b><p className="mt-2 text-sm text-[#aab9d7]">في أول نسخة، كل السهرة مبنية حول فيديو YouTube واحد متزامن.</p></div><label className="block text-sm text-[#cbd8f1]">رابط فيديو YouTube<input required className="mt-2 w-full rounded-xl border border-white/10 bg-[#0d1629] px-4 py-3 text-white" placeholder="https://youtube.com/watch?v=…" dir="ltr" value={contentUrl} onChange={event => setContentUrl(event.target.value)} /></label>{error && <p className="rounded-xl bg-[#ff7b8d]/15 px-3 py-2 text-sm text-[#ffd6dd]">{error}</p>}<button className="rounded-full bg-[#90e4ff] px-6 py-3 font-bold text-[#10172b] hover:bg-[#cff4ff]">افتح البارتي وخد كود الدعوة</button></form></section></main>; }
